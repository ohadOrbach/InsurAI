# =============================================================================
# InsurAI - Environment Configuration Template
# =============================================================================
# Copy this file to .env and fill in your values:
#   cp .env.example .env
#
# ⚠️  SECURITY: Never commit your .env file to git!
# =============================================================================

# -----------------------------------------------------------------------------
# LLM Provider Configuration (REQUIRED)
# -----------------------------------------------------------------------------
# Options: "google" (recommended), "openai", "anthropic", "mock"
LLM_PROVIDER=google

# Google Gemini API Key
# Get your free key from: https://aistudio.google.com/apikey
GOOGLE_API_KEY=your_google_api_key_here

# LLM Model
# Google: gemini-2.0-flash (recommended), gemini-1.5-pro
# OpenAI: gpt-4o, gpt-4o-mini
# Anthropic: claude-3-5-sonnet-20241022
LLM_MODEL=gemini-2.0-flash
LLM_TEMPERATURE=0.7
LLM_MAX_TOKENS=1024

# -----------------------------------------------------------------------------
# Alternative LLM Providers (Optional - uncomment if using)
# -----------------------------------------------------------------------------
# OPENAI_API_KEY=your_openai_api_key_here
# ANTHROPIC_API_KEY=your_anthropic_api_key_here

# -----------------------------------------------------------------------------
# Embedding Configuration
# -----------------------------------------------------------------------------
# Options: "gemini" (recommended, uses GOOGLE_API_KEY), "bge" (open source), "openai"
EMBEDDING_PROVIDER=gemini
EMBEDDING_MODEL=models/text-embedding-004

# -----------------------------------------------------------------------------
# Database Configuration (REQUIRED)
# -----------------------------------------------------------------------------
# PostgreSQL with pgvector extension
# Format: postgresql://USERNAME:PASSWORD@HOST:PORT/DATABASE
DATABASE_URL=postgresql://your_user:your_password@localhost:5432/insur
VECTOR_STORE_TYPE=pgvector

# -----------------------------------------------------------------------------
# OCR Settings
# -----------------------------------------------------------------------------
USE_MOCK_OCR=false
OCR_USE_GPU=false
OCR_LANGUAGE=en

# -----------------------------------------------------------------------------
# Server Settings
# -----------------------------------------------------------------------------
DEBUG=true
HOST=0.0.0.0
PORT=8000

# -----------------------------------------------------------------------------
# RAG Settings (Optional - uses sensible defaults)
# -----------------------------------------------------------------------------
# RAG_CHUNK_SIZE=2500
# RAG_CHUNK_OVERLAP=200
# RAG_TOP_K=5
